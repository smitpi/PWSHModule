# PWSHModule: Issues

---

| Catagory       | File                                | details                                                                                                                                                                                                                                                                                                                                     |
| -------------- | ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
|                |                                     |                                                                                                                                                                                                                                                                                                                                             |
| ScriptAnalyzer | PWSHModule.psd1                     | [Warning](PSAlignAssignmentStatement)L 102: Assignment statements are not aligned                                                                                                                                                                                                                                                           |
| ScriptAnalyzer | Add-PWSHModule.ps1                  | [Warning](PSAvoidUsingWriteHost)L 107: File 'Add-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.                  |
| ScriptAnalyzer | Add-PWSHModule.ps1                  | [Warning](PSAvoidUsingWriteHost)L 107: File 'Add-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.                  |
| ScriptAnalyzer | Add-PWSHModule.ps1                  | [Warning](PSAvoidUsingWriteHost)L 107: File 'Add-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.                  |
| ScriptAnalyzer | Add-PWSHModule.ps1                  | [Warning](PSAvoidUsingWriteHost)L 174: File 'Add-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.                  |
| ScriptAnalyzer | Add-PWSHModule.ps1                  | [Warning](PSAvoidUsingWriteHost)L 174: File 'Add-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.                  |
| ScriptAnalyzer | Add-PWSHModule.ps1                  | [Warning](PSAvoidUsingWriteHost)L 174: File 'Add-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.                  |
| ScriptAnalyzer | Add-PWSHModule.ps1                  | [Warning](PSAvoidUsingWriteHost)L 175: File 'Add-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.                  |
| ScriptAnalyzer | Add-PWSHModule.ps1                  | [Warning](PSAvoidUsingWriteHost)L 175: File 'Add-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.                  |
| ScriptAnalyzer | Add-PWSHModule.ps1                  | [Warning](PSAvoidUsingWriteHost)L 175: File 'Add-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.                  |
| ScriptAnalyzer | Add-PWSHModule.ps1                  | [Warning](PSAvoidUsingWriteHost)L 191: File 'Add-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.                  |
| ScriptAnalyzer | Add-PWSHModule.ps1                  | [Warning](PSAvoidUsingWriteHost)L 191: File 'Add-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.                  |
| ScriptAnalyzer | Add-PWSHModule.ps1                  | [Warning](PSAvoidUsingWriteHost)L 191: File 'Add-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.                  |
| ScriptAnalyzer | Add-PWSHModuleDefaultsToProfile.ps1 | [Warning](PSAvoidUsingWriteHost)L 125: File 'Add-PWSHModuleDefaultsToProfile.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information. |
| ScriptAnalyzer | Add-PWSHModuleDefaultsToProfile.ps1 | [Warning](PSAvoidUsingWriteHost)L 125: File 'Add-PWSHModuleDefaultsToProfile.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information. |
| ScriptAnalyzer | Add-PWSHModuleDefaultsToProfile.ps1 | [Warning](PSAvoidUsingWriteHost)L 125: File 'Add-PWSHModuleDefaultsToProfile.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information. |
| ScriptAnalyzer | Install-PWSHModule.ps1              | [Warning](PSAvoidUsingWriteHost)L 113: File 'Install-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.              |
| ScriptAnalyzer | Install-PWSHModule.ps1              | [Warning](PSAvoidUsingWriteHost)L 114: File 'Install-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.              |
| ScriptAnalyzer | Install-PWSHModule.ps1              | [Warning](PSAvoidUsingWriteHost)L 115: File 'Install-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.              |
| ScriptAnalyzer | Install-PWSHModule.ps1              | [Warning](PSAvoidUsingWriteHost)L 116: File 'Install-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.              |
| ScriptAnalyzer | Install-PWSHModule.ps1              | [Warning](PSAvoidUsingWriteHost)L 169: File 'Install-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.              |
| ScriptAnalyzer | Install-PWSHModule.ps1              | [Warning](PSAvoidUsingWriteHost)L 169: File 'Install-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.              |
| ScriptAnalyzer | Install-PWSHModule.ps1              | [Warning](PSAvoidUsingWriteHost)L 169: File 'Install-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.              |
| ScriptAnalyzer | Install-PWSHModule.ps1              | [Warning](PSAvoidUsingWriteHost)L 169: File 'Install-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.              |
| ScriptAnalyzer | Install-PWSHModule.ps1              | [Warning](PSAvoidUsingWriteHost)L 169: File 'Install-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.              |
| ScriptAnalyzer | Install-PWSHModule.ps1              | [Warning](PSAvoidUsingWriteHost)L 175: File 'Install-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.              |
| ScriptAnalyzer | Install-PWSHModule.ps1              | [Warning](PSAvoidUsingWriteHost)L 175: File 'Install-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.              |
| ScriptAnalyzer | Install-PWSHModule.ps1              | [Warning](PSAvoidUsingWriteHost)L 175: File 'Install-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.              |
| ScriptAnalyzer | Install-PWSHModule.ps1              | [Warning](PSAvoidUsingWriteHost)L 175: File 'Install-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.              |
| ScriptAnalyzer | Install-PWSHModule.ps1              | [Warning](PSAvoidUsingWriteHost)L 181: File 'Install-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.              |
| ScriptAnalyzer | Install-PWSHModule.ps1              | [Warning](PSAvoidUsingWriteHost)L 181: File 'Install-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.              |
| ScriptAnalyzer | Install-PWSHModule.ps1              | [Warning](PSAvoidUsingWriteHost)L 181: File 'Install-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.              |
| ScriptAnalyzer | Install-PWSHModule.ps1              | [Warning](PSAvoidUsingWriteHost)L 181: File 'Install-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.              |
| ScriptAnalyzer | Install-PWSHModule.ps1              | [Warning](PSAvoidUsingWriteHost)L 194: File 'Install-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.              |
| ScriptAnalyzer | Install-PWSHModule.ps1              | [Warning](PSAvoidUsingWriteHost)L 194: File 'Install-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.              |
| ScriptAnalyzer | Install-PWSHModule.ps1              | [Warning](PSAvoidUsingWriteHost)L 194: File 'Install-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.              |
| ScriptAnalyzer | Install-PWSHModule.ps1              | [Warning](PSAvoidUsingWriteHost)L 194: File 'Install-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.              |
| ScriptAnalyzer | Install-PWSHModule.ps1              | [Warning](PSAvoidUsingWriteHost)L 208: File 'Install-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.              |
| ScriptAnalyzer | Install-PWSHModule.ps1              | [Warning](PSAvoidUsingWriteHost)L 208: File 'Install-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.              |
| ScriptAnalyzer | Install-PWSHModule.ps1              | [Warning](PSAvoidUsingWriteHost)L 208: File 'Install-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.              |
| ScriptAnalyzer | Install-PWSHModule.ps1              | [Warning](PSAvoidUsingWriteHost)L 208: File 'Install-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.              |
| ScriptAnalyzer | Install-PWSHModule.ps1              | [Warning](PSAvoidUsingWriteHost)L 208: File 'Install-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.              |
| ScriptAnalyzer | Install-PWSHModule.ps1              | [Warning](PSAvoidUsingWriteHost)L 212: File 'Install-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.              |
| ScriptAnalyzer | Install-PWSHModule.ps1              | [Warning](PSAvoidUsingWriteHost)L 212: File 'Install-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.              |
| ScriptAnalyzer | Install-PWSHModule.ps1              | [Warning](PSAvoidUsingWriteHost)L 212: File 'Install-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.              |
| ScriptAnalyzer | Install-PWSHModule.ps1              | [Warning](PSAvoidUsingWriteHost)L 212: File 'Install-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.              |
| ScriptAnalyzer | Move-PWSHModuleBetweenScope.ps1     | [Warning](PSAvoidUsingWriteHost)L 100: File 'Move-PWSHModuleBetweenScope.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.     |
| ScriptAnalyzer | Move-PWSHModuleBetweenScope.ps1     | [Warning](PSAvoidUsingWriteHost)L 100: File 'Move-PWSHModuleBetweenScope.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.     |
| ScriptAnalyzer | Move-PWSHModuleBetweenScope.ps1     | [Warning](PSAvoidUsingWriteHost)L 100: File 'Move-PWSHModuleBetweenScope.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.     |
| ScriptAnalyzer | Move-PWSHModuleBetweenScope.ps1     | [Warning](PSAvoidUsingWriteHost)L 100: File 'Move-PWSHModuleBetweenScope.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.     |
| ScriptAnalyzer | New-PWSHModuleList.ps1              | [Warning](PSAvoidUsingWriteHost)L 133: File 'New-PWSHModuleList.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.              |
| ScriptAnalyzer | New-PWSHModuleList.ps1              | [Warning](PSAvoidUsingWriteHost)L 133: File 'New-PWSHModuleList.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.              |
| ScriptAnalyzer | New-PWSHModuleList.ps1              | [Warning](PSAvoidUsingWriteHost)L 133: File 'New-PWSHModuleList.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.              |
| ScriptAnalyzer | New-PWSHModuleList.ps1              | [Warning](PSAvoidUsingWriteHost)L 147: File 'New-PWSHModuleList.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.              |
| ScriptAnalyzer | New-PWSHModuleList.ps1              | [Warning](PSAvoidUsingWriteHost)L 147: File 'New-PWSHModuleList.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.              |
| ScriptAnalyzer | New-PWSHModuleList.ps1              | [Warning](PSAvoidUsingWriteHost)L 147: File 'New-PWSHModuleList.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.              |
| ScriptAnalyzer | Remove-PWSHModule.ps1               | [Warning](PSAvoidUsingWriteHost)L 153: File 'Remove-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.               |
| ScriptAnalyzer | Remove-PWSHModule.ps1               | [Warning](PSAvoidUsingWriteHost)L 153: File 'Remove-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.               |
| ScriptAnalyzer | Remove-PWSHModule.ps1               | [Warning](PSAvoidUsingWriteHost)L 153: File 'Remove-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.               |
| ScriptAnalyzer | Remove-PWSHModule.ps1               | [Warning](PSAvoidUsingWriteHost)L 131: File 'Remove-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.               |
| ScriptAnalyzer | Remove-PWSHModule.ps1               | [Warning](PSAvoidUsingWriteHost)L 131: File 'Remove-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.               |
| ScriptAnalyzer | Remove-PWSHModule.ps1               | [Warning](PSAvoidUsingWriteHost)L 131: File 'Remove-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.               |
| ScriptAnalyzer | Remove-PWSHModule.ps1               | [Warning](PSAvoidUsingWriteHost)L 131: File 'Remove-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.               |
| ScriptAnalyzer | Remove-PWSHModule.ps1               | [Warning](PSAvoidUsingWriteHost)L 125: File 'Remove-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.               |
| ScriptAnalyzer | Remove-PWSHModule.ps1               | [Warning](PSAvoidUsingWriteHost)L 125: File 'Remove-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.               |
| ScriptAnalyzer | Remove-PWSHModule.ps1               | [Warning](PSAvoidUsingWriteHost)L 125: File 'Remove-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.               |
| ScriptAnalyzer | Remove-PWSHModule.ps1               | [Warning](PSAvoidUsingWriteHost)L 122: File 'Remove-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.               |
| ScriptAnalyzer | Remove-PWSHModule.ps1               | [Warning](PSAvoidUsingWriteHost)L 122: File 'Remove-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.               |
| ScriptAnalyzer | Remove-PWSHModule.ps1               | [Warning](PSAvoidUsingWriteHost)L 122: File 'Remove-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.               |
| ScriptAnalyzer | Remove-PWSHModuleList.ps1           | [Warning](PSAvoidUsingWriteHost)L 105: File 'Remove-PWSHModuleList.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.           |
| ScriptAnalyzer | Remove-PWSHModuleList.ps1           | [Warning](PSAvoidUsingWriteHost)L 105: File 'Remove-PWSHModuleList.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.           |
| ScriptAnalyzer | Remove-PWSHModuleList.ps1           | [Warning](PSAvoidUsingWriteHost)L 105: File 'Remove-PWSHModuleList.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.           |
| ScriptAnalyzer | Save-PWSHModule.ps1                 | [Warning](PSAvoidUsingWriteHost)L 130: File 'Save-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.                 |
| ScriptAnalyzer | Save-PWSHModule.ps1                 | [Warning](PSAvoidUsingWriteHost)L 131: File 'Save-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.                 |
| ScriptAnalyzer | Save-PWSHModule.ps1                 | [Warning](PSAvoidUsingWriteHost)L 132: File 'Save-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.                 |
| ScriptAnalyzer | Save-PWSHModule.ps1                 | [Warning](PSAvoidUsingWriteHost)L 133: File 'Save-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.                 |
| ScriptAnalyzer | Save-PWSHModule.ps1                 | [Warning](PSAvoidUsingWriteHost)L 175: File 'Save-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.                 |
| ScriptAnalyzer | Save-PWSHModule.ps1                 | [Warning](PSAvoidUsingWriteHost)L 175: File 'Save-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.                 |
| ScriptAnalyzer | Save-PWSHModule.ps1                 | [Warning](PSAvoidUsingWriteHost)L 175: File 'Save-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.                 |
| ScriptAnalyzer | Save-PWSHModule.ps1                 | [Warning](PSAvoidUsingWriteHost)L 175: File 'Save-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.                 |
| ScriptAnalyzer | Save-PWSHModule.ps1                 | [Warning](PSAvoidUsingWriteHost)L 181: File 'Save-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.                 |
| ScriptAnalyzer | Save-PWSHModule.ps1                 | [Warning](PSAvoidUsingWriteHost)L 181: File 'Save-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.                 |
| ScriptAnalyzer | Save-PWSHModule.ps1                 | [Warning](PSAvoidUsingWriteHost)L 181: File 'Save-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.                 |
| ScriptAnalyzer | Save-PWSHModule.ps1                 | [Warning](PSAvoidUsingWriteHost)L 181: File 'Save-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.                 |
| ScriptAnalyzer | Save-PWSHModule.ps1                 | [Warning](PSAvoidUsingWriteHost)L 189: File 'Save-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.                 |
| ScriptAnalyzer | Save-PWSHModule.ps1                 | [Warning](PSAvoidUsingWriteHost)L 189: File 'Save-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.                 |
| ScriptAnalyzer | Save-PWSHModule.ps1                 | [Warning](PSAvoidUsingWriteHost)L 189: File 'Save-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.                 |
| ScriptAnalyzer | Save-PWSHModule.ps1                 | [Warning](PSAvoidUsingWriteHost)L 189: File 'Save-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.                 |
| ScriptAnalyzer | Save-PWSHModule.ps1                 | [Warning](PSAvoidUsingWriteHost)L 195: File 'Save-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.                 |
| ScriptAnalyzer | Save-PWSHModule.ps1                 | [Warning](PSAvoidUsingWriteHost)L 195: File 'Save-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.                 |
| ScriptAnalyzer | Save-PWSHModule.ps1                 | [Warning](PSAvoidUsingWriteHost)L 195: File 'Save-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.                 |
| ScriptAnalyzer | Save-PWSHModule.ps1                 | [Warning](PSAvoidUsingWriteHost)L 195: File 'Save-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.                 |
| ScriptAnalyzer | Save-PWSHModule.ps1                 | [Warning](PSAvoidUsingWriteHost)L 212: File 'Save-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.                 |
| ScriptAnalyzer | Save-PWSHModule.ps1                 | [Warning](PSAvoidUsingWriteHost)L 212: File 'Save-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.                 |
| ScriptAnalyzer | Save-PWSHModule.ps1                 | [Warning](PSAvoidUsingWriteHost)L 212: File 'Save-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.                 |
| ScriptAnalyzer | Save-PWSHModule.ps1                 | [Warning](PSAvoidUsingWriteHost)L 212: File 'Save-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.                 |
| ScriptAnalyzer | Save-PWSHModule.ps1                 | [Warning](PSAvoidUsingWriteHost)L 215: File 'Save-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.                 |
| ScriptAnalyzer | Save-PWSHModule.ps1                 | [Warning](PSAvoidUsingWriteHost)L 215: File 'Save-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.                 |
| ScriptAnalyzer | Save-PWSHModule.ps1                 | [Warning](PSAvoidUsingWriteHost)L 215: File 'Save-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.                 |
| ScriptAnalyzer | Save-PWSHModule.ps1                 | [Warning](PSAvoidUsingWriteHost)L 215: File 'Save-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.                 |
| ScriptAnalyzer | Save-PWSHModule.ps1                 | [Warning](PSAvoidUsingWriteHost)L 215: File 'Save-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.                 |
| ScriptAnalyzer | Save-PWSHModuleList.ps1             | [Warning](PSAvoidUsingWriteHost)L 104: File 'Save-PWSHModuleList.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.             |
| ScriptAnalyzer | Save-PWSHModuleList.ps1             | [Warning](PSAvoidUsingWriteHost)L 104: File 'Save-PWSHModuleList.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.             |
| ScriptAnalyzer | Save-PWSHModuleList.ps1             | [Warning](PSAvoidUsingWriteHost)L 104: File 'Save-PWSHModuleList.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.             |
| ScriptAnalyzer | Uninstall-PWSHModule.ps1            | [Warning](PSAvoidUsingWriteHost)L 142: File 'Uninstall-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.            |
| ScriptAnalyzer | Uninstall-PWSHModule.ps1            | [Warning](PSAvoidUsingWriteHost)L 142: File 'Uninstall-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.            |
| ScriptAnalyzer | Uninstall-PWSHModule.ps1            | [Warning](PSAvoidUsingWriteHost)L 142: File 'Uninstall-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.            |
| ScriptAnalyzer | Uninstall-PWSHModule.ps1            | [Warning](PSAvoidUsingWriteHost)L 153: File 'Uninstall-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.            |
| ScriptAnalyzer | Uninstall-PWSHModule.ps1            | [Warning](PSAvoidUsingWriteHost)L 153: File 'Uninstall-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.            |
| ScriptAnalyzer | Uninstall-PWSHModule.ps1            | [Warning](PSAvoidUsingWriteHost)L 153: File 'Uninstall-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.            |
| ScriptAnalyzer | Uninstall-PWSHModule.ps1            | [Warning](PSAvoidUsingWriteHost)L 153: File 'Uninstall-PWSHModule.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.            |
|                |                                     |                                                                                                                                                                                                                                                                                                                                             |

---

*Updated: Monday, 19 September 2022 00:40:18 UTC*
